POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=123456
POSTGRES_DB=dataflowbi

# Optional: overrides the POSTGRES_* values above.
# DATABASE_URL=postgresql+psycopg://postgres:123456@localhost:5432/dataflowbi

# Optional: import every uploaded/processed dataframe into PostgreSQL.
AUTO_IMPORT_DB=0
IMPORT_IF_EXISTS=replace
